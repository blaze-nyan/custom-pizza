const ShowPhoto = () => {
  return (
    <button
      onClick={() => console.log("clicked")}
      className="absolute right-7 top-6"
    >
      Show Photo
    </button>
  );
};

export default ShowPhoto;
